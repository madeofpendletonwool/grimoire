# binaries
/grimoire
/bin/
/dist/

# local data + index (built at runtime)
/data/
/runtime/
*.db
*.db-wal
*.db-shm

# env / secrets
.env
.env.*
!.env.example

# editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# upstream art packs — see scripts/build-assets.py. What ships is the derived
# output in web/static/assets, not the packs themselves.
/icon-packs/
