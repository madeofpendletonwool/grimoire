name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache: true

      - name: Verify formatting
        run: |
          out=$(gofmt -l .)
          if [ -n "$out" ]; then
            echo "gofmt would reformat:"; echo "$out"; exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Test
        # The default 10m per-package timeout is a hang detector, not a
        # performance budget. The race-instrumented canon and server
        # suites legitimately run 9-11 minutes on 2-core runners (every
        # test boots SQLite and replays the full migration set), so the
        # ceiling is raised rather than the tests trimmed.
        run: go test -race -count=1 -timeout 30m ./...

      # The DOM-free front-end modules (ADR 14) — today the window manager's
      # layout tree. No install step: there are no dependencies, and the root
      # package.json exists only to mark the .js files as ES modules.
      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Test front-end layout engine
        run: node --test "jstest/**/*.test.js"
